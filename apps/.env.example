# AWS & EKS
export AWS_REGION="us-east-1"
export CLUSTER_NAME="dev-eks"

# Route53
export PUBLIC_HOSTED_ZONE="seudominio.com"
export PUBLIC_HOSTED_ZONE_ID="Z1234567890ABC"

# ACME / Let's Encrypt
export ACME_EMAIL="ops@seudominio.com"

# IAM Role ARNs (outputs do Terraform)
export CERT_MANAGER_IAM_ROLE_ARN="arn:aws:iam::123456789012:role/irsa-cert-manager"
export EXTERNAL_DNS_IAM_ROLE_ARN="arn:aws:iam::123456789012:role/irsa-external-dns"
export LOKI_IAM_ROLE_ARN="arn:aws:iam::123456789012:role/irsa-loki"
export TEMPO_IAM_ROLE_ARN="arn:aws:iam::123456789012:role/irsa-tempo"
export GRAFANA_IAM_ROLE_ARN="arn:aws:iam::123456789012:role/irsa-grafana"

# S3 Buckets (outputs do Terraform)
export LOKI_S3_BUCKET="cloud-native-eks-ha-stack-dev-loki"
export TEMPO_S3_BUCKET="cloud-native-eks-ha-stack-dev-tempo"

# Grafana
export GRAFANA_ADMIN_PASSWORD="ChangeMeInProduction123!"

# Alertmanager (opcional se não usar Slack)
export SLACK_WEBHOOK_URL="https://hooks.slack.com/services/YOUR/WEBHOOK/URL"